# Angular Frontend

_Angular 21_

- Komponenten generieren: `ng generate component xxx`
- Interface/Typ generieren: `ng generate interface xxx`
- Service generieren: `ng generate service xxx`

### Build & Serve App:

`ng serve`
